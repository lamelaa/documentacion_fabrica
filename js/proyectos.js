AOS.init({
    duration: 800, // Duración en milisegundos para la animación
    once: true, // La animación solo se ejecutará una vez cuando el elemento entre en el área visible
    easing: 'ease-in-out', // Tipo de suavizado para la animación, proporcionando un inicio y un final suaves
});
